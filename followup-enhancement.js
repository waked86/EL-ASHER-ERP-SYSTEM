/* v25: WhatsApp follow-ups + equipment receipt estimate reminders */
(function(){
 const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
 function list(){return STORE.followups||(STORE.followups=[])}
 function ensure(){if(!STORE.followups)STORE.followups=[];return STORE.followups}
 function notify(f){f.last_notified=Date.now();saveStore();const title='تنبيه متابعة مطلوب';const body=f.kind==='equipment_estimate'?`إذن استلام المعدة ${f.ref||''}: اعمل المقايسة الآن`:`متابعة مطلوبة: ${f.title}`;if('Notification' in window&&Notification.permission==='granted')new Notification(title,{body});const old=document.getElementById('followup-toast');if(old)old.remove();const d=document.createElement('div');d.id='followup-toast';d.style='position:fixed;bottom:18px;left:18px;z-index:99999;background:#12233f;color:#fff;padding:16px 20px;border-radius:12px;box-shadow:0 5px 25px #0005;max-width:360px';d.innerHTML=`<b>${title}</b><div style="margin:7px 0">${esc(body)}</div><button class="btn" onclick="document.getElementById('followup-toast').remove();goTo('sales')">فتح المتابعات</button>`;document.body.appendChild(d);setTimeout(()=>d.remove(),15000)}
 function check(){const now=Date.now();ensure().filter(f=>!f.done&&(!f.next_due||new Date(f.next_due).getTime()<=now)&&(!f.last_notified||now-f.last_notified>=7200000)).forEach(notify)}
 window.renderFollowupsSub=function(el){const fs=ensure();el.innerHTML=`<div class="card"><h3>متابعة واتساب واتصالات العملاء</h3><div class="small-note">سجّل أي طلب أو وعد للعميل، وسيظهر تنبيه كل ساعتين حتى يتم إغلاق المتابعة.</div><div class="toolbar" style="flex-wrap:wrap;margin-top:10px"><input class="field" id="fu-title" placeholder="موضوع المتابعة" style="flex:1;min-width:220px"><input class="field" id="fu-customer" placeholder="اسم العميل"><input class="field" id="fu-due" type="datetime-local"><input class="field" id="fu-note" placeholder="التفاصيل" style="flex:1"><button class="btn" id="fu-add">+ حفظ متابعة</button></div><button class="btn ghost" id="fu-notify">تفعيل إشعارات المتصفح</button></div><div class="card" style="margin-top:16px"><h3>المتابعات المفتوحة</h3><div class="scroll-table"><table><thead><tr><th>النوع</th><th>العميل/المرجع</th><th>الموضوع</th><th>موعد التنبيه</th><th>الحالة</th><th></th></tr></thead><tbody>${fs.filter(f=>!f.done).sort((a,b)=>String(a.next_due).localeCompare(String(b.next_due))).map(f=>`<tr><td>${f.kind==='equipment_estimate'?'مقايسة مطلوبة':'واتساب/اتصال'}</td><td>${esc(f.customer||f.ref||'-')}</td><td>${esc(f.title)}</td><td>${f.next_due?new Date(f.next_due).toLocaleString('ar-EG'):'فوراً ثم كل ساعتين'}</td><td>${f.last_notified?'تم تنبيه سابقاً':'في الانتظار'}</td><td><button class="btn ghost" onclick="completeFollowup('${f.id}')">تمت المتابعة</button></td></tr>`).join('')||'<tr><td colspan="6">لا توجد متابعات مفتوحة</td></tr>'}</tbody></table></div></div>`;document.getElementById('fu-add').onclick=()=>{const title=document.getElementById('fu-title').value.trim();if(!title)return alert('اكتب موضوع المتابعة');fs.push({id:uid('fu'),kind:'whatsapp',title,customer:document.getElementById('fu-customer').value.trim(),note:document.getElementById('fu-note').value.trim(),next_due:document.getElementById('fu-due').value?new Date(document.getElementById('fu-due').value).toISOString():new Date().toISOString(),done:false});saveStore();renderFollowupsSub(el)};document.getElementById('fu-notify').onclick=()=>{if('Notification' in window)Notification.requestPermission().then(()=>alert('تم تفعيل إشعارات المتصفح'))}}
 window.completeFollowup=id=>{const f=ensure().find(x=>x.id===id);if(f){f.done=true;f.completed_at=new Date().toISOString();saveStore();goTo('sales')}};
 const orig=window.submitEquipmentReceipt;if(typeof orig==='function'){window.submitEquipmentReceipt=function(){const before=(STORE.equipment_receipts||[]).length;const out=orig.apply(this,arguments);const after=STORE.equipment_receipts||[];if(after.length>before){const r=after[after.length-1];ensure().push({id:uid('fu'),kind:'equipment_estimate',title:'إنشاء مقايسة صيانة',customer:r.customer_name,ref:r.doc_no,note:'تم استلام معدة وتحتاج إلى مقايسة',next_due:new Date().toISOString(),done:false});saveStore()}return out}}
 setInterval(check,60000);setTimeout(check,3000);setTimeout(()=>{if(typeof SECTIONS!=='undefined'&&SECTIONS.sales&&!SECTIONS.sales.subs.some(s=>s.key==='followups'))SECTIONS.sales.subs.push({key:'followups',label:'المتابعات والتنبيهات',render:window.renderFollowupsSub})},0);
})();
